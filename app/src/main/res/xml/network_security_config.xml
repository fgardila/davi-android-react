<?xml version="1.0" encoding="utf-8"?>
<!--
  Certificate pinning is omitted because there is no real backend server — all
  network traffic is intercepted locally by MockInterceptor before it leaves the
  device. When a real backend is introduced, add <pin-set> entries here.
-->
<network-security-config>
    <!-- Block all cleartext traffic in release builds -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
      Metro bundler needs cleartext WebSocket (ws://) to deliver hot-reload and
      the dev menu. 10.0.2.2 is the emulator's alias for the host machine;
      localhost covers physical devices using `adb reverse`. Both addresses are
      unreachable from the internet, so this domain-config is safe in release too.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
